// src/console/log/session-log.ts
// Full implementation in Task 9
import type { App } from "obsidian";
import type { SessionConfig, RoundState } from "../console.types";

export async function appendRoundToLog(
    _app: App,
    _config: SessionConfig,
    _state: RoundState,
    _roundCloseText: string
): Promise<void> {
    // Stub — full implementation in Task 9
}
